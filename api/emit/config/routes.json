{
  "routes": [
    {
      "method": "GET",
      "path": "/emits",
      "handler": "emit.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emits/count",
      "handler": "emit.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emits/:id",
      "handler": "emit.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/emits",
      "handler": "emit.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/emits/:id",
      "handler": "emit.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/emits/:id",
      "handler": "emit.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
