'use strict';
const refresh = async (ctx) => {
    
    // car = await strapi.query('product-car').create({
    //     user: user
    //   });
    // const result = await strapi.services.scrapper.getEmited({
    //     rut: '16.593.992-1',
    //     clave: 'Felipe23'
    // });
    // return result;
}


module.exports = {
    refresh
};
